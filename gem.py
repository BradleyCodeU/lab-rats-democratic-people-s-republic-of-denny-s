class GemStone():
    def _inti_(self, color, glow):
        self.color = color #red
        self.glow = glowOFF # true false
        self.glowOFF = True
        self.isCLEAN = False

        def get_interface(self, heldItems, current_room):
            if self.isCLEAN = true
                self.glowOFF == false
                print("The gem glows in your hand, with the warm GLOW of approval")
            elif self.isCLEAN = false
                print("if you clean the gem to activate the gem")
            else
                print("if you clean the gem to activate the gem")
                
            
                
                    
        def check_input(self, command, heldItems, current_room):
             if command == "CLEAN":
                 self.turn_on()
             
            
                        
                        
            
            
        
                 
